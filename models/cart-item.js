class CartItem {
  constructor(key, quantity, productPrice, productTitle, sum) {
    this.key = key;
    this.quantity = quantity;
    this.productPrice = productPrice;
    this.productTitle = productTitle;
    this.sum = sum;
  }
}
